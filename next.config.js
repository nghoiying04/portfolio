/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/portfolio',
  assetPrefix: '/portfolio/',
  swcMinify: false,
  output: 'export',
  reactStrictMode: true
}

module.exports = nextConfig
